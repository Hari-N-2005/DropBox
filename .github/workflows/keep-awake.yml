name: Keep Render App Awake

on:
  schedule:
    # Runs every 13 minutes
    - cron: '*/13 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Send HTTP request to Render app
        run: |
          curl -s https://dropbox-z82f.onrender.com/api/test
